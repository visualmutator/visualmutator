<UserControl x:Class="PiotrTrzpil.VisualMutator_VSPackage.Views.UnitTestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ViewModels="clr-namespace:PiotrTrzpil.VisualMutator_VSPackage.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid d:DataContext="{d:DesignInstance ViewModels:UnitTestsViewModel}">
        <ListView Grid.ColumnSpan="2" AllowDrop="True" SelectionMode="Extended" ItemsSource="{Binding Mutants}"
              IsManipulationEnabled="True">

            <ListView.View>
                <GridView>
                    <GridViewColumn Width="400" Header="Name"
                          DisplayMemberBinding="{Binding Mode=TwoWay, Path=Name}"/>
                    <GridViewColumn Width="300" Header="Date" DisplayMemberBinding="{Binding Date}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
