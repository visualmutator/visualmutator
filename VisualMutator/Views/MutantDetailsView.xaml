<UserControl x:Class="VisualMutator.Views.MutantDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Views="clr-namespace:VisualMutator.Views" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>

        <TextBlock  Grid.Row="0"  Text="Tests for selected mutant:" />
        <TabControl >
            <TabItem Header="Tests">
                <Views:TestsTree Content="{Binding TestsTree}"/>

              
           
            </TabItem>

            <TabItem Header="Mutated Code">
                <Views:UnitTestsView Content="{Binding UnitTestsView}"/>
            </TabItem>

        </TabControl>

       
    </Grid>
</UserControl>
